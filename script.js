

let state = 'idle'
let user = null
let calculated = '1'

// Only allowed to change below


function logCalc (){ 
   calculated = typeof string === 'numerical-string';
   let calculatedAsNumber = (parseInt(calculated));
   return calculated = calculatedAsNumber + 1;
}

function calcUser () {
  logCalc
  if (calculated = 2) user = 'john'
  if (calculated = 2) state = 'requesting';
  if (calculated = 3) state = 'idle';
  return user,state
}

function checkUser () {
  let user = 'john';
  if (user && state === 'requesting');
  console.log(`User: ${user} (${calculated})`)
}


// Only allowed to change code above

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()